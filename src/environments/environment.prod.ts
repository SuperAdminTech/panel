import { version } from '../../package.json';

export const environment = {
  url: 'https://api.caste.qbitartifacts.com',
  realm: 'default',
  production: true,
  brand: {
    title: 'Caste',
  },
  debug: false,
  version,
};

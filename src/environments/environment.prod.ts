import { version } from '../../package.json';

export const environment = {
  url: 'https://api.logic.qbitartifacts.com',
  production: true,
  brand: {
    title: 'Trading',
  },
  debug: false,
  version,
};
